%h1= t(".title")

= link_to t(".new_post"), stormy.new_admin_post_path

%table.table
  %thead
    %tr
      %th= t(".name")
      %th= t(".published")
      %th= t(".permalink")
      %th= t(".published_at")
  %tbody
    - @posts.each do |post|
      %tr
        %td= link_to post.name, stormy.edit_admin_post_path(post)
        %td
          = post.published? ? t(".yes") : t(".no")
          - if post.published? && post.published_at > Time.now
            = t(".future")
        %td= link_to post_path(post), post_path(post), "data-no-turbolink" =>  true
        %td= post.published_at && post.published_at.strftime(t("stormy.date.formats.short")) if post.published?
